CREATE TABLE `ecms_richtextdata` (
    `id` integer AUTO_INCREMENT NOT NULL PRIMARY KEY,
    `header` varchar(100) NOT NULL,
    `homepage_header` varchar(100) NOT NULL,
    `rich_text_page_id` integer NOT NULL
) CHARSET=utf8
;
